import { Forum, Inter } from "next/font/google"

export const forum = Forum({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
})

export const inter = Inter({
  weight: "300",
  subsets: ["latin"],
  display: "swap",
})